# 2026-03-13

## Gemini CLI Integration
- Integrated Gemini CLI framework v0.11.0.
- Restored README.md from the commit prior to Gemini CLI integration.
- Implemented a complete documentation suite in `/docs` (index, user-guide, design, deploy, and develop) covering the library's architecture and usage patterns.
- Created `docs/skill.md` as a specialized technical guide for AI coding agents to use `lingo-ai` idiomatically.
- Implemented CI/CD workflow for MkDocs rendering and GitHub Pages deployment on every release.
- Replaced `black` with `ruff` for linting and formatting, and updated `makefile` and GitHub Actions accordingly.
- Fixed linting errors and reformatted code using `ruff`.
